# НовыйГодЧат V9

Новогодний чат с музыкой, котами, реакциями, профилями, достижениями, streak, интерактивными эффектами и общей ёлкой.

## Обновление D1
После загрузки V9 выполни `init-db.sql`, чтобы создать таблицы `reactions` и `tree_stars`.

Старые мемы удалены из V9-архива. Если они уже есть в GitHub, см. `REMOVE_FROM_GITHUB.txt`.
